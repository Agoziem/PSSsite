// export function useState(initialValue, render) {
//   let state = initialValue; // State variable
//   const setState = (newValue) => {
//     // Setter function
//     state = newValue;
//     render(); // Re-render the UI after state change
//   };
//   return [state, setState];
// }
